apiVersion: v1
kind: Service
metadata:
  name: bert-nodeport-service
  namespace: serviceplaying
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: MyApp
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30007