apiVersion: v1
kind: Service

metadata:
  name: nginx-nodeport

spec:
  type: NodePort
  selector:
    app: bertinx
  ports:
  - port: 80  # This is the Port of the Service inside Kubernetes
    targetPort: 80  # This is the Container Port
    nodePort: 30080  # This is the Port from the Host.