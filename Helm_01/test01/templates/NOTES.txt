***************************************
- Webserver deployment with 3 replicas
- Configmap with a number of keys
- Simple Ubuntu pod for debugging called "shell"
- Gateway API to the Webserver

Access the webserver:
- From the outside: http://webserver.bert/ (config this in /etc/hosts pointing to gateway IP)
- From the inside: webserver.helmp.svc.cluster.local
