apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmmicroservice.fullname" . }}
  labels:
    {{- include "helmmicroservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmmicroservice.selectorLabels" . | nindent 4 }}
